# 🤖 Butler Android — 方案 B: Chaquopy + 原生 UI

> **技术栈**: Kotlin + Jetpack Compose + Chaquopy (Python 3.10)
> **平台**: Android 8.0+ (API 26+)
> **架构**: 原生 UI ↔ Python 技能引擎 (进程内调用)

## 架构

```
┌─────────────────────────────────────┐
│         Jetpack Compose UI          │
│  ┌─────────┐ ┌──────────┐ ┌──────┐ │
│  │ ChatScreen│ │SkillsScreen│ │Settings│ │
│  └────┬────┘ └────┬─────┘ └──┬───┘ │
│       │           │          │      │
│  ┌────▼───────────▼──────────▼───┐  │
│  │      ButlerBridge (Kotlin)    │  │
│  │  ┌─────────────────────────┐  │  │
│  │  │   Chaquopy Python VM    │  │  │
│  │  │   butler_android.py     │  │  │
│  │  │   SkillManager          │  │  │
│  │  └─────────────────────────┘  │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 构建

```bash
# 需要 Android Studio + SDK 34
cd butler-android-b
./gradlew assembleDebug

# APK 输出: app/build/outputs/apk/debug/app-debug.apk
```

## 功能

- 💬 原生 Chat 界面 (Compose)
- 📦 技能列表浏览
- ⚙️ 设置 (服务器地址、API Key)
- 🐍 Python 技能引擎 (Chaquopy 进程内调用)
- 🔌 离线可用 (本地 Python 执行)
